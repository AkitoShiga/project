import Vue from 'vue'
import Vuex from 'vuex'
import Axios from 'axios'
Vue.use(Vuex)
const state = {
}
const actions = {
}
const mutations = {
}
const store = new Vuex.Store({
    state: state,
    actions: actions,
    mutations: mutations,
});
export default store;

